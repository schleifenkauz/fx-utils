plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}
rootProject.name = 'fx-utils'

if (file("../reaktive").exists()) {
    includeBuild("../reaktive")
}
if (file("../kollektion").exists()) {
    includeBuild("../kollektion")
}